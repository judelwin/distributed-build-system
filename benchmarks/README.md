# Benchmarks

This directory contains scripts and results for evaluating the performance of the distributed build system.

- Run experiments with different cluster sizes
- Collect build latency and cache statistics
- Store CSV results for analysis
